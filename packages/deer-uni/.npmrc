# pnpm + uniapp 兼容：@dcloudio 构建依赖平铺解析，符号链接会导致找不到模块
# 仅对 deer-uni 生效，不污染 monorepo 其他包
shamefully-hoist=true

# 非交互环境（无 TTY）下允许自动移除旧 modules 目录
confirmModulesPurge=false

# 国内镜像加速
registry=https://registry.npmmirror.com/
